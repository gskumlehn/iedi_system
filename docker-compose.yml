services:
  web:
    build: .
    image: iedi-system:latest
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./:/app
      - ./data:/app/data
    command: >
      gunicorn --bind 0.0.0.0:8080 --workers 2 --threads 4 wsgi:app
